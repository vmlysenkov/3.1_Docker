version: '3.7'

services:

  db:
    image: postgres:latest
    ports:
      - '5432:5432'
    volumes:
      - ./data:/var/lib/postgresql/data
    environment:
      - POSTGRES_DB=app_db
      - POSTGRES_USER=tiger
      - POSTGRES_PASSWORD=vasya21